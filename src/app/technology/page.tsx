import { RouteStub } from "@/components/route-stub";

export default function TechnologyPage() {
  return (
    <RouteStub
      eyebrow="Technology"
      title="The science behind the signal."
      hint="Mosaic hero and tech-feature blocks arrive in phase 2."
    />
  );
}
