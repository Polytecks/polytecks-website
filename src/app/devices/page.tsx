import { RouteStub } from "@/components/route-stub";

export default function DevicesPage() {
  return (
    <RouteStub
      eyebrow="Devices"
      title="Clinical and veterinary platforms."
      hint="Pill-tab switcher (Clinical / Veterinary) and product blocks arrive in phase 2."
    />
  );
}
