import { RouteStub } from "@/components/route-stub";

export default function CareersPage() {
  return (
    <RouteStub
      eyebrow="Careers"
      title="Help us make the invisible legible."
      hint="Role placeholder grid arrives in phase 2."
    />
  );
}
