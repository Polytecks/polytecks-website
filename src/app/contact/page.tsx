import { RouteStub } from "@/components/route-stub";

export default function ContactPage() {
  return (
    <RouteStub
      eyebrow="Contact"
      title="Start a conversation."
      hint="Channel placeholder cards arrive in phase 2."
    />
  );
}
