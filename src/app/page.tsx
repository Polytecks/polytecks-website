import { RouteStub } from "@/components/route-stub";

export default function HomePage() {
  return (
    <RouteStub
      eyebrow="Home"
      title="Making the Skin a Window into the Body"
      hint="Hero, mission panel, partners ribbon, and topographic canvas land here in phase 2 + 3."
    />
  );
}
